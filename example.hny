format multiboot


video: u8<> = 0xb8000 as u8<> # moreover, you can cast now! #
# now you can assign %
% globals to sth!    #

fn main() {
	video[0] = 'X'
	halt()
	leave # comment #
}

fn halt() {
	# ever loop #
	forever {
		ignore(0)
	}
}
